[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "cd backend && pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "bash start.sh"

