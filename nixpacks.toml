[phases.setup]
nixPkgs = { python = "311" }

[phases.install]
cmds = [
  "cd backend && pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1"

